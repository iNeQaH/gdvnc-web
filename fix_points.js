const fs = require('fs'); let content = fs.readFileSync('src/app/api/support/points/route.ts', 'utf8'); content = content.replace(/const isSuperAdmin = user.role === 'ADMIN' \|\| user.username === 'iNeQaH';/g, ''); content = content.replace('if (!user) {', 'const isSuperAdmin = user.role === \'ADMIN\' || user.username === \'iNeQaH\';\n    if (!user) {'); fs.writeFileSync('src/app/api/support/points/route.ts', content);
