const fs = require('fs'); let content = fs.readFileSync('src/app/api/support/points/route.ts', 'utf8'); content = content.replace(/const isSuperAdmin = .*?\\n/, ''); content = content.replace('if (!user) {\\n      return NextResponse.json({ success: false, error: \\'Không tìm th?y tài kho?n.\\' }, { status: 404 });\\n    }', 'if (!user) {\\n      return NextResponse.json({ success: false, error: \\'Không tìm th?y tài kho?n.\\' }, { status: 404 });\\n    }\\n\\n    const isSuperAdmin = user.role === \\'ADMIN\\' || user.username === \\'iNeQaH\\';'); fs.writeFileSync('src/app/api/support/points/route.ts', content);
